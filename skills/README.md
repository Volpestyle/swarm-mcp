# Packaged Skills

This directory contains skills shipped to consumers with the `swarm-mcp` package.

- `swarm-mcp` is the packaged coordination skill.

Repo-internal skills live under `.agents/skills/` and are not part of the published package by default.
