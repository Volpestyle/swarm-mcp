# Repo-Internal Skills

These skills are for working inside this repository. They are not packaged as consumer skills by default.

- `swarm-deepdive` investigates this repo's swarm database, UI queue, and server logs.
- `tauri` supports work on the Tauri desktop/mobile app code.

The packaged consumer skill lives at `skills/swarm-mcp`.
